import { NextPage } from "next";
import ContextTemp from "../components/templates/ContextTem";
import { createContext } from "react";

const UseContext : NextPage = () => {
    return(
        <ContextTemp/>
    );
};
export default UseContext;